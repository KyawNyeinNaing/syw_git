mkdir foldername
cd foldername
nano

git status (show status)

git add . (all file add)
git commit -m "something"
git log (show commits)
git branch (show branch)
git branch branch_name (create branch)
git branch -D branch_name (delete branch)
git checkout branch_name (checkout to branch)
git chekcout master (checkout to master)
git push origin master (push to master)
git pull (pull from amster)
git clone https://github.com/bmstd/bmstd.git
:wq (resolve conflit)

git merge branch_name (added all file with branches)

